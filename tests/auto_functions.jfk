int simple() {
    return 42;
}

int add(int a, int b) {
    return a + b;
}

float calculateAverage(int count, float sum) {
    return sum / count;
}

print simple();

int sum;
sum = add(5, 7);
print sum;

float avg = calculateAverage(3, 15.0);
print avg;
